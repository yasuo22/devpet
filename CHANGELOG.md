# Changelog

DevPet 版本变更记录。

## [1.0.0] - 2026-08-12

> 🎉 **首个正式 Release：`v1.0.0`** —— 本版本经正式评审合并流程发布，作为 DevPet 的稳定基线版本。

### 正式发布说明

- **版本标签**：`v1.0.0`
- **发布方式**：经 PR 代码评审 → 合并至 `main` 正式主分支 → 打 tag 发布
- **兼容**：纯静态 Web 版（`devpet/`）+ Tauri 2 桌面壳（`tauri/`）双形态

### ✨ 新增（petdex 生态对齐）

- **Pet 元数据扩展**：新增 `kind`（creature / object / character）与 `vibes`（cozy / calm / playful / ...）字段，对齐 petdex 宠物包格式，为接入社区生态铺路。
- **编码活动反应**：新增 `codingActivity.js` 模块，监听 Codex token 消耗增量（真实编码活动信号）——检测到 coding agent 工作 → 宠物进入 `working` 状态并弹出鼓励泡泡、奖励亲密度；空闲后自动回到 `idle`。
- **宠物编辑器 UI**：设置面板新增「类型（Kind）」下拉与「气质（Vibes）」输入，可视化编辑并实时预览。
- **主题市场预设补全**：所有内置预设（DevPet / 蓝莓 / 桃桃 / 芽芽 / 小夜 / 花狸）补充 kind / vibes 元数据，网格展示 vibe 标签。
- **导入导出携带新字段**：宠物配置导出/导入 JSON 兼容 kind / vibes，并做合法性校验。

### 🔧 优化

- 编码活动泡泡增加冷却时间，避免刷屏打扰。
- 主题市场卡片布局优化，展示 vibe 标签。

### 🐛 修复

- 版本号统一为 1.0.0（此前 devpet 核心与 tauri 壳存在 1.1.0 不一致）。
- 宠物导入/导出对新元数据字段的兼容与降级。

---

## 迭代历史（发布前）

以下为 1.0.0 之前的迭代开发记录，供追溯。

### v0.x 系列迭代

- **第六步**：Codex 真实 token 接入 + 猫粮购买交易系统 + 宠物成长系统 + 番茄钟联动
- **第五步**：彩色狸花猫（追蝴蝶 / 睡猫窝）+ 猫粮系统（token → 猫粮，4 小时提醒）
- **第四步**：开发者控制中心（泡泡优先级队列 + 主题市场 + 通知服务 Webhook + 协作模式）
- **第三步**：番茄钟系统通知桌面壳联动 + 宠物编辑器 UI + 主题切换
- **第二步**：Tauri 2 桌面壳搭建（全局置顶浮窗 / 点击穿透 / 托盘 / 通知）
- **第一步**：Pet 元数据 Schema + Widget 拖拽 + 贡献热图 + GitHub 账号关联
- **初始**：DevPet 桌面宠物应用初始化
